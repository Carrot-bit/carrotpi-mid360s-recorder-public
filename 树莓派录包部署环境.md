# 树莓派 4B Mid-360S 只录包部署环境

更新时间：2026-08-20（Asia/Shanghai）  
用途：树莓派只负责接收 Mid-360S 数据并录制 ROS 2 bag；回到室内后，将原始 bag 转移到笔记本，再使用干净 FAST-LIO 基线回放和运行论文实验。

## 1. 目标架构

```text
Mid-360S --千兆以太网--> Raspberry Pi 4B (eth0)
                              |
                              +-- wlan0 --> 手机热点 / SSH 控制
                              |
                              +-- USB 3 SSD --> 原始 bag

回到室内：SSD/bag --> 笔记本 --> 干净 FAST-LIO + 后续实验算法
```

树莓派不运行 FAST-LIO 建图、不运行 RViz、不运行压缩算法。这样可以降低移动采集时的 CPU、内存和散热压力。

## 2. 推荐硬件

- Raspberry Pi 4B，建议 8GB RAM；4GB 也可作为只录包节点尝试。
- 主动散热：带风扇的散热片或官方主动散热方案。不要把树莓派放在完全密闭、无通风的包内。
- 稳定 5V/3A 或更高质量电源。避免电压不足、欠压和突然重启。
- USB 3 SSD 作为 bag 存储。不要把长时间、高写入量的正式 bag 持续写入 microSD 卡。
- 可靠的短千兆网线。让网线从接口自然垂下并留出松弛弯，避免插头承受侧向拉力。
- 如使用电池供电，电池、升压/降压模块和树莓派的额定电流必须满足持续 5V 供电；正式采集前先做长时间通电测试。

## 3. 推荐操作系统

推荐：

- Ubuntu Server 22.04 LTS 64-bit（ARM64/aarch64）
- ROS 2 Humble

可选：Ubuntu Desktop 22.04 64-bit，但录包节点不需要图形桌面，Server 版更省资源。

不建议作为本实验的首次部署目标：

- 32 位 Raspberry Pi OS
- 仅为省空间而使用的非 22.04/非 Humble 组合
- 在录包树莓派上安装并运行 RViz、FAST-LIO 或完整桌面开发环境

检查系统架构：

```bash
uname -m
```

期望输出：

```text
aarch64
```

检查系统版本：

```bash
lsb_release -a
```

期望为 Ubuntu 22.04 LTS 系列。

## 4. ROS 2 软件环境

必须安装或可用：

- ROS 2 Humble 基础运行环境
- `ros-humble-ros-base`
- `ros-humble-rosbag2`
- `ros-humble-rosbag2-storage-default-plugins`
- `ros-humble-rclpy`
- `ros-humble-sensor-msgs`
- `python3-colcon-common-extensions`
- `python3-rosdep`
- 编译 Livox 驱动所需的 C++/CMake 工具链

建议安装的通用工具：

```bash
sudo apt update
sudo apt install -y \
  git build-essential cmake pkg-config \
  python3-colcon-common-extensions python3-rosdep \
  python3-pip \
  ros-humble-ros-base \
  ros-humble-rosbag2 \
  ros-humble-rosbag2-storage-default-plugins \
  ros-humble-rclpy \
  ros-humble-sensor-msgs \
  openssh-server iw ethtool pciutils usbutils lm-sensors
```

如果某些 ROS 包已经随 Humble 基础安装提供，重复安装不会造成问题。`ethtool`、`lm-sensors` 等工具用于采集网卡、温度和硬件状态证据。

验证 ROS：

```bash
source /opt/ros/humble/setup.bash
ros2 --version
printenv ROS_DISTRO
```

期望 `ROS_DISTRO=humble`。

## 5. Livox 驱动环境

树莓派需要单独建立 ARM64 ROS 2 工作空间并编译 `livox_ros_driver2`。建议使用与笔记本当前可工作的 ROS 2 分支/版本一致的源码，至少确认：

- 可启动 `msg_MID360s_launch.py`；
- 使用 `MID360s_config.json`；
- 发布 `/livox/lidar`，类型为 `livox_ros_driver2/msg/CustomMsg`；
- 发布 `/livox/imu`，类型为 `sensor_msgs/msg/Imu`；
- LiDAR 目标频率约 10 Hz；
- IMU 目标频率约 200 Hz。

建议工作空间形式：

```text
/home/<pi-user>/carrot_ws/
  src/livox_ros_driver2/
  build/
  install/
  log/
```

编译前：

```bash
source /opt/ros/humble/setup.bash
cd /home/<pi-user>/carrot_ws
rosdep install --from-paths src --ignore-src -r -y
colcon build --symlink-install --cmake-args -DCMAKE_BUILD_TYPE=Release
source install/setup.bash
```

注意：树莓派的驱动配置文件必须使用树莓派有线网卡地址作为 host IP；不要直接假设笔记本配置可以原样复制。

## 6. 网络配置

推荐拓扑：

```text
Mid-360S：REPLACE_MID360S_IP
树莓派 eth0：REPLACE_PI_ETH_IP/24
手机热点：树莓派 wlan0，通过 DHCP 获取地址
```

现场验证记录（2026-08-21）：树莓派连接手机热点后，`wlan0` 获得
`REPLACE_HOTSPOT_DHCP_IP`。这是 DHCP 临时地址，不写入 Netplan；热点重启、设备重连
或租约变化后，应以热点客户端列表中的 `pi-host` 地址为准。

自动网络冷启动实测（2026-08-21）：家用 Wi-Fi 与手机热点同时可见时，
Pi 连接手机热点；NetworkManager 自动连接优先级为热点 `100`、家用 Wi-Fi
`10`。`eth0` 保持 `REPLACE_PI_ETH_IP/24`，到 `REPLACE_MID360S_IP` 的路由使用
`eth0` 且 carrier 为 `1`。优先级服务为 `Type=oneshot`，执行成功后显示
`inactive (dead)` 属正常状态，应以 `status=0/SUCCESS` 判断成功。
关闭手机热点后再次冷启动，Pi 已实测自动回退家用 Wi-Fi，优先级仍保持
`100/10`，雷达静态地址、路由和 carrier 均未受影响。

有线雷达链路和手机 SSH 链路分开：

- `eth0` 只连接 Mid-360S；
- `wlan0` 连接手机热点；
- 手机通过 `wlan0` 的地址 SSH 到树莓派；
- 不要让手机热点或 Wi-Fi 路由替代雷达到树莓派的有线 UDP 链路。

查看接口：

```bash
ip -br addr
ip route
cat /sys/class/net/eth0/carrier
```

期望 `eth0` 为 `UP`，且 `carrier` 为 `1`。如果实际接口名不是 `eth0`，以 `ip -br addr` 输出为准，并同步修改录包脚本配置。

确认雷达数据包到达：

```bash
sudo tcpdump -ni eth0 'udp port 56300 or udp port 56400' -c 20
```

## 7. 存储与性能依据

本机已测得的参考负载：

- 以太网接收约 24 Mb/s；
- bag 持续写入约 3.9 MB/s；
- 约 4 分钟 bag 约 920 MB。

因此 Pi 4B 的千兆网口在带宽上有充足余量，但 microSD 卡的持续写入稳定性和寿命不适合作为正式数据存储。USB 3 SSD 应挂载到固定目录，例如：

```text
/mnt/rosbags
```

检查持续空间：

```bash
df -h /mnt/rosbags
```

正式采集前至少保留几十 GiB 可用空间，并为每个 bag 保留独立目录，禁止覆盖已有数据。

## 8. 温度、电源和系统状态

录包期间建议每秒或每几秒记录：

- CPU 温度；
- 欠压/降频状态；
- 系统负载和内存；
- `eth0` carrier、RX packets/bytes、drop/error；
- ROS 话题最近消息年龄。

常用检查：

```bash
vcgencmd measure_temp 2>/dev/null || true
vcgencmd get_throttled 2>/dev/null || true
cat /sys/class/thermal/thermal_zone0/temp
free -h
uptime
```

如果 `vcgencmd get_throttled` 报告非零状态，或温度持续过高、系统频繁降频，应先处理散热/供电，不得直接录正式数据。

## 9. 录包节点软件边界

树莓派只需要运行：

1. Livox ROS 2 驱动；
2. `ros2 bag record /livox/lidar /livox/imu`；
3. 采集看门狗；
4. 温度、网卡和磁盘状态记录；
5. SSH 服务。

树莓派不需要运行：

- FAST-LIO 建图；
- RViz；
- map compression 算法；
- 参数调优或正式评价脚本。

笔记本上的 `/home/carrotuser/小论文1/实验1/快捷脚本/bag.sh` 是笔记本专用脚本，不能直接复制后就执行。树莓派应创建单独的 Pi 录包脚本，至少替换：用户路径、bag 根目录、日志根目录、接口名 `eth0`、驱动工作空间和温度/电源检测命令。

## 10. 首次部署验收顺序

不要第一次开机就直接录正式 bag。按以下顺序验收：

1. 确认 `aarch64`、Ubuntu 22.04、ROS 2 Humble。
2. 确认 `eth0` 与 Mid-360S 的静态地址互通，`carrier=1`。
3. 确认手机热点和 SSH 可用。
4. 用 `tcpdump` 确认 56300/56400 UDP 数据到达。
5. 启动 Livox 驱动，确认两个话题类型和消息到达。
6. 分别测量 LiDAR/IMU 频率，目标约 10/200 Hz。
7. 在 USB 3 SSD 上录制 60 秒测试 bag。
8. 检查 `ros2 bag info`、消息数量、频率、磁盘文件和 SHA-256。
9. 连续录制 10～15 分钟，确认没有 `carrier=0`、RX drop/error、温度异常、欠压或 bag 写入停顿。
10. 只在以上检查通过后，开始正式 bagA/bagC 采集。

## 11. 回传笔记本后的完整性检查

每个树莓派 bag 至少应携带：

- bag 目录（`*.db3` 和 `metadata.yaml`）；
- 录制 session 日志；
- 看门狗 TSV 和异常证据（如有）；
- `ros2 bag info` 输出；
- SHA-256 文件。

复制到笔记本后：

```bash
sha256sum -c sha256.txt
ros2 bag info /path/to/copied_bag
```

哈希不一致时，停止后续 LIO 实验，保留原始树莓派文件并重新复制/校验。不要在笔记本上覆盖原始 bag。

## 12. 与论文基线的关系

树莓派只是改变采集设备，不改变 FAST-LIO 算法。正式基线仍然是：

- `<laptop-fast-lio-workspace>`
- clean commit：`2fffc570a25d0df172720bac034fbdb6a13d2162`
- 外部配置：`map_compression/configs/baseline_mid360s.yaml`

只要树莓派保存的是原始 `/livox/lidar` 和 `/livox/imu`，并且时间戳、消息类型、频率和完整性通过验收，后续在笔记本上回放不影响“未侵入 FAST-LIO 算法本身、仅做设备适配/参数调整”的基线定义。

## 13. 当前状态

- 本文档是部署规格和验收清单，不代表树莓派已经安装完成。
- 尚未在树莓派实机验证 ARM64 ROS 2、Livox 驱动、SSD 持续写入和长时间温度表现。
- 配置树莓派时如遇到无法判断的系统/驱动版本冲突，应暂停并记录完整命令输出，不要自行替换论文基线或修改 FAST-LIO 源码。

## 14. 无头重装与实际介质说明

本节是本项目的实际执行手册。当前电脑识别到的介质为：

| 用途 | 当前识别 | 处理规则 |
|---|---|---|
| Raspberry Pi SD | 约 29.7 GiB，当前含 `bootfs`/`rootfs` | 允许擦除并重刷；写卡前按型号、容量、序列号再次确认 |
| Ubuntu 启动 U 盘 | SanDisk 约 28.7 GiB，amd64 Ubuntu 22.04 | 不能直接给 Pi 启动；写卡时拔下，避免误选 |
| `carrot_disk` | external NTFS disk `<ntfs-partition>`，NTFS，UUID `REPLACE_NTFS_UUID` | 不格式化、不分区、不运行 `ntfsfix`，保留全部既有文件 |

树莓派不能直接启动普通 x86/amd64 Ubuntu U 盘。该 U 盘只能启动电脑，或者用于下载/准备 Pi 专用 ARM64 镜像。写 SD 卡前拔掉 Ubuntu U 盘和 `carrot_disk`，只连接 SD 读卡器；任何 `dd` 命令都必须使用经过 `lsblk`、型号和序列号确认的 SD 设备，不能照抄 `/dev/sda`。

### 14.1 写入 Pi ARM64 系统

在 Ubuntu 电脑上安装 Raspberry Pi Imager（只在电脑上运行）：

```bash
sudo apt update
sudo apt install -y rpi-imager
rpi-imager
```

在 Imager 中选择 Raspberry Pi 4、Ubuntu Server 22.04 LTS 64-bit（ARM64/+raspi），选择 SD 卡并写入。若使用手工下载的镜像，必须从同一官方发布页下载该镜像的 SHA-256 清单，并在写卡前执行 `sha256sum -c <官方清单>`；不要把未校验的镜像交给写卡工具。高级设置中预置 `pi-host`、用户 `carrotuser`、时区 `Asia/Shanghai`、SSH 公钥和家用 Wi-Fi。笔记本和手机分别生成 Ed25519 公钥，只把两个 `.pub` 文件写入镜像，绝不复制私钥。

写入完成后先不要插入 `carrot_disk`。插入 SD 启动 Pi，等待两分钟，在家用 Wi-Fi DHCP 客户端列表中查找 `pi-host`，或尝试：

```bash
ssh carrotuser@pi-host.local
```

首次登录后验证两台控制设备都能用各自密钥登录，再关闭密码登录。确认两个 SSH 会话都仍可用后，在 Pi 上执行：

```bash
sudo install -d -m 0755 /etc/ssh/sshd_config.d
printf '%s\n' \
  'PasswordAuthentication no' \
  'KbdInteractiveAuthentication no' \
  'PermitRootLogin no' \
  'PubkeyAuthentication yes' | sudo tee /etc/ssh/sshd_config.d/60-carrot-keys-only.conf >/dev/null
sudo sshd -t && sudo systemctl reload ssh
```

若 `.local` 不可用，使用路由器 DHCP 地址；不要猜测地址。

### 14.2 安装依赖与部署资产

在 Pi 上执行：

```bash
sudo timedatectl set-timezone Asia/Shanghai
sudo apt update
sudo apt install -y \
  openssh-server avahi-daemon network-manager netplan.io rsyslog \
  git build-essential cmake pkg-config python3-colcon-common-extensions \
  python3-rosdep python3-pip tcpdump ethtool iw pciutils usbutils \
  lm-sensors ntfs-3g \
  ros-humble-ros-base ros-humble-rosbag2 \
  ros-humble-rosbag2-storage-default-plugins ros-humble-rclpy python3-yaml \
  ros-humble-sensor-msgs ros-humble-pcl-conversions libpcl-dev
sudo rosdep init 2>/dev/null || true
rosdep update
```

将本目录的 `pi_deployment/` 复制到 Pi，执行：

```bash
cd pi_deployment
sudo ./install.sh
```

编辑 `/etc/carrot-net/home.yaml` 和 `/etc/carrot-net/field.yaml`，填入真实 SSID/密码。家用 Wi-Fi 和手机热点都不能使用 `REPLACE_RADAR_SUBNET`，否则会和 Mid-360S 链路冲突。

### 14.3 外接盘挂载

只对确认过 UUID 的 NTFS 分区添加 fstab 行；不要使用 `/dev/sdX`：

```bash
sudo mkdir -p /mnt/carrot_disk
sudo cp /etc/fstab /etc/fstab.carrot-backup
echo 'UUID=REPLACE_NTFS_UUID /mnt/carrot_disk ntfs-3g rw,nofail,x-systemd.automount,x-systemd.device-timeout=10s,uid=1000,gid=1000,umask=0022,windows_names 0 0' | sudo tee -a /etc/fstab
sudo findmnt --verify --verbose
```

在 Ubuntu 22.04 上，`findmnt --verify` 可能把 fstab 中的用户态类型名 `ntfs-3g` 与盘上类型 `ntfs` 静态比较，并报告 `ntfs-3g seems unsupported by the current kernel` 和 `does not match with on-disk ntfs`。只要实际挂载后 `findmnt` 显示 UUID 正确、`FSTYPE=fuseblk`、选项含 `rw`，并且 `carrot-storage-guard` 通过，这属于 libmount 的类型名提示，不是卷错误；不得因此运行 `ntfsfix` 或改用未验证的修复操作。

首次挂载必须验证真实来源和文件系统：

```bash
sudo mount /mnt/carrot_disk
findmnt -M /mnt/carrot_disk -o SOURCE,FSTYPE,UUID,OPTIONS
sudo mkdir -p /mnt/carrot_disk/carrot_rosbags
```

`carrot-storage-guard` 会在每次录制前再次检查 UUID、真实挂载、读写状态、写探针和剩余空间。程序只主动创建 `carrot_rosbags`，不会递归修改或删除其他目录。NTFS 全卷仍由同一 Unix 用户可写；这属于程序约束，不是操作系统级目录隔离，断电或文件系统损坏仍可能影响未备份文件。

### 14.4 Livox ARM64 编译

将已验证的 `livox_ros_driver2` 源码快照和 Livox-SDK2 提交 `REPLACE_LIVOX_SDK2_COMMIT` 复制到 Pi。不能复制电脑 `/usr/local/lib` 中的 x86-64 SDK。该公开模板不包含任何硬件专用的源码清单或哈希；在目标 Pi 上重新生成并保存到本地私有配置。

```bash
git clone https://github.com/Livox-SDK/Livox-SDK2.git /tmp/Livox-SDK2
cd /tmp/Livox-SDK2
git checkout REPLACE_LIVOX_SDK2_COMMIT
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build -j2
sudo cmake --install build
sudo ldconfig

mkdir -p /home/carrotuser/carrot_ws/src
# 将精确的 livox_ros_driver2 快照放入 /home/carrotuser/carrot_ws/src/
source /opt/ros/humble/setup.bash
cd /home/carrotuser/carrot_ws
rosdep install --from-paths src --ignore-src -r -y
colcon build --symlink-install --event-handlers console_direct+ \
  --cmake-args -DHUMBLE_ROS=humble -DCMAKE_BUILD_TYPE=Release
source install/setup.bash
```

在启动驱动前校验来源，而不是手工修改后继续使用：

```bash
cd /home/carrotuser/carrot_ws/src
sha256sum -c /usr/local/share/carrot-recorder/livox_ros_driver2.files.sha256
sha256sum livox_ros_driver2/config/MID360s_config.json \
  livox_ros_driver2/launch_ROS2/msg_MID360s_launch.py
```

后两项必须分别为 `REPLACE_CONFIG_SHA256` 和 `REPLACE_LAUNCH_SHA256`。`carrot-driver.service` 启动时会重新执行此检查；校验失败则拒绝启动，因此录制也会拒绝。确认 `MID360s_config.json` 的 host IP 是 `REPLACE_PI_ETH_IP`、雷达 IP 是 `REPLACE_MID360S_IP`，并使用 `msg_MID360s_launch.py`，不要替换成普通 Mid-360 配置。

### 14.5 网络模式与切换

```bash
sudo carrot-net select home
sudo reboot
```

在家用网络完成更新和构建后，配置现场模式：

```bash
sudo carrot-net select field
sudo reboot
```

安装器会移除 cloud-init 的 Netplan YAML；选择器拒绝在发现其他活动 YAML 时继续，以保证配置档互斥。每次选择前确认只有 `99-carrot.yaml`，并且家用 Wi-Fi、手机热点都不使用 `REPLACE_RADAR_SUBNET`：

```bash
find /etc/netplan -maxdepth 1 -type f -name '*.yaml' -printf '%f\n'
```

切换命令只替换下次启动的 YAML，不会立即断开当前 SSH。重启后 Pi 通过手机热点获得 SSH 地址，`eth0` 固定为 `REPLACE_PI_ETH_IP/24`，默认路由仍由 wlan0 提供。检查：

```bash
sudo carrot-net status
ip route get REPLACE_MID360S_IP
cat /sys/class/net/eth0/carrier
```

`carrot-net status` 会在输出前将 Netplan 的 `password:` 字段替换为
`***`；仍不要在聊天、工单或公开日志中粘贴未经确认的网络配置原文。

应显示 `dev eth0 src REPLACE_PI_ETH_IP` 和 `carrier=1`。救援时拔掉雷达、笔记本直连 eth0，并选择 `rescue`；笔记本端配置 `REPLACE_RESCUE_HOST_IP/24`，Pi 地址为 `REPLACE_RESCUE_PI_IP`。

若需要像笔记本一样自动选择网络，先在 Pi 本地根据已受保护的
`home.yaml` 和 `field.yaml` 生成 `auto` 档，再选择它：

```bash
sudo carrot-net create-auto
sudo carrot-net select auto
sudo reboot
```

`auto` 始终给 `eth0` 配置 `REPLACE_PI_ETH_IP/24`，并为两个 Wi-Fi 生成独立的
NetworkManager 连接。手机热点的自动连接优先级为 100，家用 Wi-Fi 为 10；
热点可见时优先连接热点，热点不可见时回退到家用 Wi-Fi。两个 Wi-Fi 都不可用
时不会连接。自动选择只在启动和 Wi-Fi 重新连接时发生，已连接的家用 Wi-Fi
不会为了后来出现的热点主动中断。要强制使用热点，关闭再开启 wlan0 或重启。

### 14.6 systemd 录包操作

首次启用用户服务常驻：

```bash
sudo loginctl enable-linger carrotuser
systemctl --user daemon-reload
```

`install.sh` 已执行上述 linger 和用户 manager 刷新，并将 `carrotuser` 加入 `adm` 组以读取新增的内核 USB/NTFS/I/O 错误；安装后必须重启一次才会获得该组权限。此处命令仅在手动更新 unit 文件时使用。
`install.sh` 同时将 `carrotuser` 加入 `video` 组，使用户录包服务能够读取 `/dev/vcio` 并执行 `vcgencmd get_throttled`；否则只有 `sudo vcgencmd` 可用，录包健康检查会失败。安装器在已完成首次 NoCloud 配置后写入 `/etc/cloud/cloud-init.disabled`，避免 cloud-init 在以后每次启动重复探测并延迟约 30 秒。

所有网络配置档都明确使用 NetworkManager，因此安装器会屏蔽 `systemd-networkd-wait-online.service`。若不屏蔽，Ubuntu 22.04 Raspberry Pi 镜像可能在每次启动时无意义等待约 120 秒，导致 Wi-Fi、Avahi 和 SSH 延后可用；`NetworkManager-wait-online.service` 保持启用。

现场启动一次录制：

```bash
carrot-bag start bagA 60
carrot-bag watch bagA
```

`start` 的可选分钟数只用于验收测试，范围为 1～60；省略时固定为 60 分钟。例如 `carrot-bag start smoke 1` 会录制一个 60 秒测试 bag，然后进入同样的 30 秒延长窗口。正式采集使用默认值或显式写 `60`。

录制服务不会因 SSH 断开而停止，也不会在重启后自动恢复。监督器以内核单调时钟计算每个 60 分钟区间，时钟校正不会改变到期时间。60 分钟到期后服务进入 30 秒延长窗口；另一 SSH 会话执行 `carrot-bag extend bagA` 等同于按 `y`，执行 `carrot-bag stop` 或 `Ctrl+C` 等同于 `n`。`y` 会增加下一个 60 分钟区间并可重复。无人回应或所有 SSH 断开时自动安全停止；普通监控界面按 `Ctrl+C` 也只请求安全停止。服务默认不允许单个区间超过 60 分钟，可提前停止。

常用命令：

```bash
carrot-bag status
carrot-bag stop
carrot-bag logs bagA
carrot-bag driver-stop
```

停止时先结束看门狗，再向 rosbag 进程组发送 `SIGINT` 并等待 SQLite 刷新；必要时才升级到 `TERM/KILL`。随后会执行 `ros2 bag info`、按记录时长检查两个话题的消息数量/频率、生成相对路径 SHA-256、`sync`，最后写入 `COMPLETE`。任何异常只会写 `FAILED`，不会续录或覆盖原目录。停止后先确认 `COMPLETE` 和 `bag_files.sha256`，再执行：

```bash
sha256sum -c /mnt/carrot_disk/carrot_rosbags/bagA_*/bag_files.sha256
sync
sudo umount /mnt/carrot_disk
```

不要在服务仍运行时拔盘；`sync` 不是卸载的替代品。

### 14.7 验收与故障演练

依次完成：架构/版本检查、SSH 双设备登录、两种网络模式冷启动、UDP 56300/56400、话题类型和 8–12/160–240 Hz、60 秒测试 bag、10–15 分钟稳定性测试，以及完整 60 分钟压力测试。压力测试必须使用实际 Pi 电源、USB 线、WD 硬盘和轻微手持工况，检查 `vcgencmd get_throttled`、`dmesg` 中无欠压/USB reset/UAS/I/O/NTFS 错误。

2026-08-21 实机验收记录：`smoke3` 成功生成 `COMPLETE`，LiDAR 为
`9.997 Hz`、IMU 为 `200.012 Hz`，相对 SHA-256 全部通过；随后
`stability10` 连续录制 `632.349 s`、生成约 `2.5 GiB` 单个 SQLite3
文件，LiDAR 为 `10.001 Hz`、IMU 为 `200.000 Hz`，哈希全部通过。
录制结束后驱动保持运行，电源状态为 `throttled=0x0`。
`lifecycle` 测试在监控界面按 `Ctrl+C` 后以 `stop_reason=operator` 安全
停止，生成 `COMPLETE`；LiDAR 为 `10.002 Hz`、IMU 为 `199.998 Hz`，
哈希通过，且 bag 停止后驱动继续运行。
`fault_carrier` 测试在录制中断开雷达网线后以
`stop_reason=carrier_down` 异常停止，保留部分 bag、metadata、哈希和系统
快照，只生成 `FAILED`，未生成 `COMPLETE`；驱动服务保持运行。
`fault_watchdog` 测试在录制中终止 watchdog 后以
`stop_reason=watchdog_exited` 异常停止，同样保留部分 bag 和系统快照，只
生成 `FAILED`；驱动服务保持运行。
`fault_recorder` 测试在录制中终止 rosbag 进程后以
`stop_reason=recorder_exited` 异常停止，保留部分 bag、metadata、哈希和
系统快照，只生成 `FAILED`；驱动服务保持运行。
`driverstop` 测试在活动录制中执行 `carrot-bag driver-stop`，bag 先以
`stop_reason=operator` 安全停止并生成 `COMPLETE`、通过哈希，随后驱动
服务停止，验证了固定的先 bag 后驱动顺序。
`fault_driver` 测试在录制中停止受管驱动后以
`stop_reason=lidar_stall` 异常停止，保留部分 bag 和系统快照，只生成
`FAILED`；驱动服务保持停止状态。
`sshdrop` 测试从驱动停止状态启动，录包服务自动启动驱动；断开 SSH 后
录制继续，最终无人响应延长窗口而以 `stop_reason=timeout` 安全停止，生成
`COMPLETE`，两话题频率与哈希均通过。
`extend_yes` 测试在到期窗口输入 `y` 后将 deadline 延长约 3600 秒，随后
由操作员安全停止并生成 `COMPLETE`、通过哈希。验收时发现监控端可能在
supervisor 确认请求前重复显示一次提示，已增加 phase 确认等待以消除竞态。
`extend_no` 测试输入 `n` 后以 `stop_reason=operator` 安全停止，生成
`COMPLETE` 并通过哈希。验收时发现停止刷新期间可能重复提示，已改为发送
stop 后等待服务实际退出，不再返回提示循环。
`nodisk` 测试在硬盘安全卸载并物理断开后，由
`ExecStartPre=carrot-storage-guard` 在启动 rosbag 和驱动前拒绝录制，未产生
活动状态或 SD 回退目录；重新接盘后 UUID、`fuseblk`、读写和空间检查恢复
正常。
`pressure60` 在真实硬件上完成了完整 60 分钟压力测试，实际录制
`3636.462 s`，生成约 `14 GiB` 的单个 SQLite3 文件。LiDAR 共
`36364` 条、频率 `10.000 Hz`，IMU 共 `727293` 条、频率
`200.000 Hz`。无人回应延长窗口后以 `stop_reason=timeout` 安全
停止，`db3` 和 `metadata.yaml` 的相对 SHA-256 均通过，只生成
`COMPLETE`、未生成 `FAILED`。结束后 `throttled=0x0`，本次启动的
内核日志中未检出欠压、USB reset、UAS、I/O 或 NTFS 错误。
最终数据验收采用直接将 WD 硬盘接到笔记本的方式，桌面自动挂载于
`/media/<user>/carrot_disk`，未复制 14 GiB 数据。`pressure60_<timestamp>`
目录中 `sha256sum -c bag_files.sha256` 全部通过；在笔记本执行
`source /opt/ros/humble/setup.bash` 后运行 `ros2 bag info`，报告单个
`13.9 GiB` SQLite3 文件、时长 `3636.461778808 s`、LiDAR `36364` 条
(`livox_ros_driver2/msg/CustomMsg`) 和 IMU `727293` 条 (`sensor_msgs/msg/Imu`)。
SSH 最终审计返回 `permitrootlogin no`、`pubkeyauthentication yes`、
`passwordauthentication no`，公钥登录和远程禁止 root/密码登录均生效。

故意拔掉硬盘、使用错误 UUID、模拟只读挂载、断开雷达网线、停止驱动或 watchdog，均应得到失败/异常结束和 SD 本地诊断日志，且不能在 SD 上生成 bag。回传笔记本后用相对 SHA-256 清单和 `ros2 bag info` 校验，不覆盖树莓派原始目录。
